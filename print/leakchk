int	main(int argc, char **argv)
{
	char *line;
	int	fd1;
	fd1 = open(argv[1], O_RDONLY);

	while(1)
	{
		line = get_next_line(fd1);
		if (line == NULL)
			break ;
		printf("len[%ld]: %s", ft_strlen(line), line);
		free(line);
	}
	close(fd1);
	return (0);
}
